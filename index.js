const currentUser = "Chrissy";
 
const punctuation = "!";

const welcomeMessage = "Welcome to Flatbook, " + currentUser + punctuation;
const excitedWelcomeMessage = "WELCOME TO FLATBOOK, " + currentUser.toUpperCase() + punctuation;
const shortGreeting = "Welcome, " + currentUser[0] + punctuation;
//other correct options
//const shortGreeting = "Welcome, " + currentUser.slice(0,1) + punctuation;
//const shortGreeting = "Welcome, " + currentUser.charAt(0) + punctuation;

//Interpolation
//const interpolatedWelcomeMessage = `Welcome to Flatbook,  ${currentUser}${punctuation}`;
//const interpolatedShortGreeting = `Welcome, ${currentUser[0]}${punctuation}`;


































































































 
